apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: bg-web
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web
    version: blue-v1
    #version: green-v2
